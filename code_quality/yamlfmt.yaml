parameters:
  - name: SOURCE_FOLDER
    type: string
    default: '.'
script: |
  echo "Set up Go from cache"
  echo "##vso[task.setvariable variable=PATH;/usr/local/go/bin:$PATH]"
  export PATH=$PATH:/usr/local/go/bin
  go version
condition: succeededOrFailed()
displayName: Use Cached Go

